[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

# pyproject.toml
[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests"
]

[tool.pytest]
cov_options = {
    "exclude": ["tests", "docs", "examples"]
}
